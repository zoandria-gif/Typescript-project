export class student {
  id: number;
  firstName: string;
  lastName: string;
  number: string;

  constructor(id: number, firstName: string, lastName: string, number: string) {
    this.id = id;
    this.firstName = firstName;
    this.lastName = lastName;
    this.number = number;
  }
}